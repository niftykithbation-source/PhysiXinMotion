name: physix_in_motion
description: "PhysiX in Motion - offline gamified Grade 11 STEM physics app"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management (locked, CLAUDE.md)
  flutter_riverpod: ^2.6.1

  # Local DB (locked, CLAUDE.md)
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.5.28
  path_provider: ^2.1.5
  path: ^1.9.0

  # Charts (locked, CLAUDE.md)
  fl_chart: ^0.69.2

  # Export/share (locked, CLAUDE.md)
  share_plus: ^10.1.4

  # Other locked deps
  uuid: ^4.5.1
  permission_handler: ^11.3.1

  # Step 6 Teacher Portal: file picker for "Import Student Results"
  # (Tier-1 sync). Not in CLAUDE.md's original locked list. Pinned to 10.x:
  # 8.x hardcodes compileSdk 34 (too low for flutter_plugin_android_
  # lifecycle); 11.x skips applying kotlin-android under AGP 9+ and expects
  # a full "built-in Kotlin" migration this project hasn't made (breaks
  # older-style plugins like share_plus that still apply kotlin-android
  # directly). 10.x has the compileSdk fix without that AGP9 branching.
  file_picker: ^10.3.3

  # Motion Lab 3D upgrade: pure Vector3/matrix math for the hand-rolled
  # pseudo-3D orbit camera in motion_3d_panel.dart (CustomPainter-drawn,
  # no WebGL/GPU engine). Already resolved transitively via fl_chart at
  # this exact version; promoted to a direct dependency since app code now
  # imports it directly rather than relying on a transitive resolution.
  vector_math: ^2.2.0

  # Dashboard / Evaluate-modal QR display (Teacher Roster Integration —
  # see the notes near the top of student_home_shell.dart). Pure on-device
  # rendering of a small local JSON summary, no network calls.
  qr_flutter: ^4.1.0

  # Teacher Portal QR scanner (Class Roster -> "Scan Student QR"). Camera
  # capture + on-device barcode decode, no network calls.
  mobile_scanner: ^7.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Drift codegen (needed once the Step 2 schema lands)
  build_runner: ^2.4.13
  drift_dev: ^2.22.1

  # Launcher icon generation (blueprint §7 Step 9), run once via
  # `dart run flutter_launcher_icons` — not a runtime dependency.
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/content/kinematics_v1.json
    - assets/icons/icon_mark_transparent.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Launcher icon source (blueprint §7 Step 9) — the glowing cyan X/orbit mark,
# not the default Flutter icon. Regenerate with `dart run flutter_launcher_icons`
# after changing any of these source files; do not edit generated icon files
# under android/.../mipmap-*/ or ios/Runner/Assets.xcassets/ directly.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "icon_full_bleed_1024.png"
  min_sdk_android: 26
  adaptive_icon_background: "icon_adaptive_background.png"
  adaptive_icon_foreground: "icon_adaptive_foreground.png"
